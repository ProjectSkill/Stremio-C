events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    sendfile on;
    keepalive_timeout 65;

    upstream stremio_backend {
        server stremio:11470;
    }

    server {
        listen 80;
        server_name _;
        
        # Stremio API proxy
        location /stremio/ {
            proxy_pass http://stremio_backend/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
        }

        # Burger menu static files
        location /burger-menu/ {
            alias /usr/share/nginx/html/burger-menu/;
            index index.html;
            try_files $uri $uri/ =404;
        }

        # Root redirect to burger menu
        location / {
            return 302 /burger-menu/;
        }
    }
}