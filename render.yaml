services:
  - type: web
    name: ultimate-player-redirect
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health