services:
  - type: web
    name: genius-stremio
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health