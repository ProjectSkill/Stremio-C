services:
  - type: web
    name: stremio-c
    env: docker  # This tells Render to use your Dockerfile
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080  # Or whatever port your app uses
    healthCheckPath: /manifest.json