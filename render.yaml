services:
  - type: web
    name: burger-menu-ultimate
    env: docker
    plan: free
    docker:
      dockerfile: Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health